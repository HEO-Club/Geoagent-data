---
description: Geo Agent Dataset 全局开发规则
alwaysApply: true
---

# 全局开发规则

1. SPEC.md（v3）是项目唯一有效的产品与工程规格来源；旧规格见 SPEC_legacy_v2.md（仅归档）。
2. 每次编码前必须阅读 SPEC.md 中与当前阶段相关的章节。
3. 严格按照 SPEC.md 定义的开发顺序推进。
4. 一次只实现用户当前明确指定的阶段，不得提前实现后续阶段。
5. 主实现位于 pipeline/；不得擅自修改 v3 公共 Schema、枚举值与阶段函数签名。
6. 如果 SPEC.md 存在冲突或无法实现的要求，停止编码并报告，不得自行猜测。
7. 所有 Python 代码必须有类型注解，公共接口必须有必要的 docstring。
8. 所有结构化 LLM 输出必须经过 Pydantic v2 校验（阶段2 仅软信封校验，不做统一 tool schema）。
9. 所有外部服务必须通过 adapter 或依赖注入调用。
10. 测试中禁止调用真实付费 API。
11. 当前阶段必须实现对应测试（tests/）。
12. 完成后必须运行当前阶段测试和 tests 全量测试。
13. 测试全部通过后停止，不得自动开始下一阶段。
14. 不得硬编码 API key、模型名、服务器地址或凭证。
15. 不得引入 LangChain 或 LangGraph 作为核心流水线框架。
16. groundtruth 不得进入阶段2 或任何轨迹生成模型的上下文。
17. 不得修改与当前阶段无关的文件。
18. **严禁用过拟合思路改项目**：不得为单条视频/单一样本发明仅对该例成立的硬门禁、特判分支或词表；发现数据质量问题应优先修正通用契约、抽取召回、训练信号与提示。若规则只对个别案例有意义，不得合入。
